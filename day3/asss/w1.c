#include <stdio.h>
void main(){
	while (1){
		int x,y;
		printf("the first num is:");
		scanf(" %d",&x);
		
		printf(" the second num is:");
		scanf(" %d",&y);
		
		printf("the result is :%d\n",x+y);
	}
}